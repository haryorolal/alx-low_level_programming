#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _puts - prints a string
 * Description: 'to prints string using put'
 * @str: argument to use
 */
void _puts(char *str)
{
puts(str);
_putchar('\n');
}
