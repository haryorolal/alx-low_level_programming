#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Print numbers'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
	{
		printf("%d", p);
	}
	putchar('\n');
	return (0);

}
